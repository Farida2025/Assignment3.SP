// ---------------------------
// Adaptee
// ---------------------------
// Existing class that works only with XML.
// It is NOT compatible with the Target interface.
class XmlParser {
    // Method to process XML data
    public String parseXml(String xmlData) {
        // In real life, this would parse XML into an object model
        return "Parsed XML: " + xmlData;
    }
}